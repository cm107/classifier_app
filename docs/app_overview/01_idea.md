Assume that we are creating a pure python project.
We want to create a GUI app using PySide6.
The goal of the app is to:
1. Provide the user with a way to create new classification datasets from scratch, manage existing datasets, combine datasets, pruning datasets, and preparing datasets for training/validation/testing.
2. Managing hyperparameters for models, training, etc.
3. Launching training and testing sessions while monitoring progress mid-session.
4. Managing trained models, allowing the user to delete or finetune model weights.
5. Providing a UI for running inference on a source image directory, video, or webcam.

Please define the directory structure and filenames for our project, assuming that all non-entrypoint files are organized under a python package folder. I will ask you to provide more detailed information about each file later, so please just give me the tree hierarchy for now.