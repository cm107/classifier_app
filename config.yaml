defaults:
  batch_size: 32
  epochs: 50
  image_size: 224
  label_smoothing: 0.1
  learning_rate: 0.001
  max_lr: 0.01
  projection_dim: 128
  temperature: 0.5
  top_k: 3
  weight_decay: 0.01
hardware:
  num_workers: 4
  use_cuda: true
paths:
  dataset_root: storage/datasets
  log_dir: storage/logs
  model_export_root: storage/models
